<div class="navbar navbar-expand-lg navbar-light navbar-lewagon">

<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <i class="fas fa-bars"></i>
  </button>


    <div class="collapse navbar-collapse" id="navbarSupportedContent">
       <ul class="navbar-nav mr-auto">
         <% if user_signed_in? %>
              <li class="nav-item active">
                <%= link_to "Home", root_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Bookshelf", user_books_path(current_user.id), class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "New Book", new_user_book_path(current_user.id), class: "nav-link" %>
              </li>
              <ul class="nav navbar-nav navbar-right">
                <li class="nav-item dropdown navbar-avatar">
                  <%= image_tag "default_avatar.png", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
                  <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <u><%="#{current_user.email}"%></u>
                    <%= link_to "Loans", user_book_loans_path(current_user.id, :book_id), class: "dropdown-item" %>
                    <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
                  </div>
                </li>
            </ul>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
</div>
