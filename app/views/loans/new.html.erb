<br>
<br>
<div class="container" style="margin-top: 10%">
  <div class="row w-100">
    <div class="col-xs-12 col-sm-8 offset-sm-2 col-lg-6 offset-lg-3 p-0">
      <div class="form-login">
       <h3 style="text-align: center;">LOAN FORM</h3>
          <%= simple_form_for(@loan, url: user_book_loans_path(@book), method: :post) do |f| %>
            <%= f.input :date, as: :string, input_html: { class: "datepicker" } %>
            <%= f.input :name %>
                  <div class="container mt-2">
                   <div class="row d-flex justify-content-center">
                    <div class="col-align-self-center">
                  <%= f.button :submit, id:"button-loan" %>
                  <%= link_to "Back", user_books_path, class: "btn btn-outline-dark"%>                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
